# CountingSort

## Track

Algorithms track: `algorithms`

Category: `sorting`

## What this algorithm does

Counts occurrences of each key to place elements in order (non-comparison).

## Planned operations

Core operations: sort, count.

## Status

- ⬜ Not implemented yet — `CountingSort` is a blank scaffold.
- Runner: `CountingSortMain` (empty `main`, ready for a quick example).

## Source

- `algorithms.sorting.countingsort.CountingSort`

## What will go here later

- The implementation of `CountingSort`.
- A small example wired into `CountingSortMain` to try it out.
- Tests under `src/test/java/algorithms/sorting/countingsort/`.
