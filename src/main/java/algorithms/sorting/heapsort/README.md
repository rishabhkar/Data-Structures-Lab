# HeapSort

## Track

Algorithms track: `algorithms`

Category: `sorting`

## What this algorithm does

Builds a heap and repeatedly extracts the root to produce sorted order.

## Planned operations

Core operations: sort, buildHeap, siftDown.

## Status

- ⬜ Not implemented yet — `HeapSort` is a blank scaffold.
- Runner: `HeapSortMain` (empty `main`, ready for a quick example).

## Source

- `algorithms.sorting.heapsort.HeapSort`

## What will go here later

- The implementation of `HeapSort`.
- A small example wired into `HeapSortMain` to try it out.
- Tests under `src/test/java/algorithms/sorting/heapsort/`.
