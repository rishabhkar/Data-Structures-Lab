# BubbleSort

## Track

Algorithms track: `algorithms`

Category: `sorting`

## What this algorithm does

Repeatedly swaps adjacent out-of-order elements until the list is sorted.

## Planned operations

Core operations: sort, compareSwap.

## Status

- ⬜ Not implemented yet — `BubbleSort` is a blank scaffold.
- Runner: `BubbleSortMain` (empty `main`, ready for a quick example).

## Source

- `algorithms.sorting.bubblesort.BubbleSort`

## What will go here later

- The implementation of `BubbleSort`.
- A small example wired into `BubbleSortMain` to try it out.
- Tests under `src/test/java/algorithms/sorting/bubblesort/`.
