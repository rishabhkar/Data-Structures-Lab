# QuickSort

## Track

Algorithms track: `algorithms`

Category: `sorting`

## What this algorithm does

Partitions around a pivot and sorts the partitions recursively.

## Planned operations

Core operations: sort, partition.

## Status

- ⬜ Not implemented yet — `QuickSort` is a blank scaffold.
- Runner: `QuickSortMain` (empty `main`, ready for a quick example).

## Source

- `algorithms.sorting.quicksort.QuickSort`

## What will go here later

- The implementation of `QuickSort`.
- A small example wired into `QuickSortMain` to try it out.
- Tests under `src/test/java/algorithms/sorting/quicksort/`.
