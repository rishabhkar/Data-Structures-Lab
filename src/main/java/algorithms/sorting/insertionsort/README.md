# InsertionSort

## Track

Algorithms track: `algorithms`

Category: `sorting`

## What this algorithm does

Builds the sorted list one element at a time by inserting each into place.

## Planned operations

Core operations: sort, insert.

## Status

- ⬜ Not implemented yet — `InsertionSort` is a blank scaffold.
- Runner: `InsertionSortMain` (empty `main`, ready for a quick example).

## Source

- `algorithms.sorting.insertionsort.InsertionSort`

## What will go here later

- The implementation of `InsertionSort`.
- A small example wired into `InsertionSortMain` to try it out.
- Tests under `src/test/java/algorithms/sorting/insertionsort/`.
