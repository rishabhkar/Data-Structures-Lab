# KMP

## Track

Algorithms track: `algorithms`

Category: `strings`

## What this algorithm does

Pattern matching using a prefix function to skip redundant re-checks.

## Planned operations

Core operations: search, buildPrefix.

## Status

- ⬜ Not implemented yet — `KMP` is a blank scaffold.
- Runner: `KMPMain` (empty `main`, ready for a quick example).

## Source

- `algorithms.strings.kmp.KMP`

## What will go here later

- The implementation of `KMP`.
- A small example wired into `KMPMain` to try it out.
- Tests under `src/test/java/algorithms/strings/kmp/`.
