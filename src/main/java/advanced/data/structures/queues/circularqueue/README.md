# CircularQueue

## Track

Advanced implementation track: `advanced-data-structures`

Category: `queues`

## What this structure is

A fixed-size ring buffer queue that reuses freed slots.

## Planned operations

Core operations: enqueue, dequeue, peek, isFull, isEmpty.

## Status

- ⬜ Not implemented yet — `CircularQueue` is a blank scaffold.
- Runner: `CircularQueueMain` (empty `main`, ready for demo calls).

## Source

- `advanced.data.structures.queues.circularqueue.CircularQueue`

## What will go here later

- The implementation of `CircularQueue`.
- A small demo wired into `CircularQueueMain`.
- Tests under `src/test/java/advanced/data/structures/queues/circularqueue/`.
