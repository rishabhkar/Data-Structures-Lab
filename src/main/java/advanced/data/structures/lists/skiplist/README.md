# SkipList

## Track

Advanced implementation track: `advanced-data-structures`

Category: `lists`

## What this structure is

A probabilistic, layered linked structure giving logarithmic ordered access.

## Planned operations

Core operations: insert, search, delete.

## Status

- ⬜ Not implemented yet — `SkipList` is a blank scaffold.
- Runner: `SkipListMain` (empty `main`, ready for demo calls).

## Source

- `advanced.data.structures.lists.skiplist.SkipList`

## What will go here later

- The implementation of `SkipList`.
- A small demo wired into `SkipListMain`.
- Tests under `src/test/java/advanced/data/structures/lists/skiplist/`.
