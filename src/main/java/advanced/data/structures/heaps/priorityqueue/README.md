# PriorityQueue

## Track

Advanced implementation track: `advanced-data-structures`

Category: `heaps`

## What this structure is

A queue that removes elements by priority, typically backed by a heap.

## Planned operations

Core operations: offer, poll, peek, changePriority.

## Status

- ⬜ Not implemented yet — `PriorityQueue` is a blank scaffold.
- Runner: `PriorityQueueMain` (empty `main`, ready for demo calls).

## Source

- `advanced.data.structures.heaps.priorityqueue.PriorityQueue`

## What will go here later

- The implementation of `PriorityQueue`.
- A small demo wired into `PriorityQueueMain`.
- Tests under `src/test/java/advanced/data/structures/heaps/priorityqueue/`.
