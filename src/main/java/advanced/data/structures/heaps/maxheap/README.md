# MaxHeap

## Track

Advanced implementation track: `advanced-data-structures`

Category: `heaps`

## What this structure is

A complete binary tree where each parent is larger than its children.

## Planned operations

Core operations: insert, extractMax, peek, heapify, increaseKey.

## Status

- ⬜ Not implemented yet — `MaxHeap` is a blank scaffold.
- Runner: `MaxHeapMain` (empty `main`, ready for demo calls).

## Source

- `advanced.data.structures.heaps.maxheap.MaxHeap`

## What will go here later

- The implementation of `MaxHeap`.
- A small demo wired into `MaxHeapMain`.
- Tests under `src/test/java/advanced/data/structures/heaps/maxheap/`.
