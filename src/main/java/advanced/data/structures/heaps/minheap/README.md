# MinHeap

## Track

Advanced implementation track: `advanced-data-structures`

Category: `heaps`

## What this structure is

A complete binary tree where each parent is smaller than its children.

## Planned operations

Core operations: insert, extractMin, peek, heapify, decreaseKey.

## Status

- ⬜ Not implemented yet — `MinHeap` is a blank scaffold.
- Runner: `MinHeapMain` (empty `main`, ready for demo calls).

## Source

- `advanced.data.structures.heaps.minheap.MinHeap`

## What will go here later

- The implementation of `MinHeap`.
- A small demo wired into `MinHeapMain`.
- Tests under `src/test/java/advanced/data/structures/heaps/minheap/`.
