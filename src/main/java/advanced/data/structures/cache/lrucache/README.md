# LRUCache

## Track

Advanced implementation track: `advanced-data-structures`

Category: `cache`

## What this structure is

A cache that evicts the least recently used entry when it is full.

## Planned operations

Core operations: get, put, evictLRU.

## Status

- ⬜ Not implemented yet — `LRUCache` is a blank scaffold.
- Runner: `LRUCacheMain` (empty `main`, ready for demo calls).

## Source

- `advanced.data.structures.cache.lrucache.LRUCache`

## What will go here later

- The implementation of `LRUCache`.
- A small demo wired into `LRUCacheMain`.
- Tests under `src/test/java/advanced/data/structures/cache/lrucache/`.
