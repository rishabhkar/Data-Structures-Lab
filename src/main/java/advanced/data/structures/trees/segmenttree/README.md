# SegmentTree

## Track

Advanced implementation track: `advanced-data-structures`

Category: `trees`

## What this structure is

A tree for efficient range queries and updates over an array.

## Planned operations

Core operations: build, rangeQuery, pointUpdate, rangeUpdate.

## Status

- ⬜ Not implemented yet — `SegmentTree` is a blank scaffold.
- Runner: `SegmentTreeMain` (empty `main`, ready for demo calls).

## Source

- `advanced.data.structures.trees.segmenttree.SegmentTree`

## What will go here later

- The implementation of `SegmentTree`.
- A small demo wired into `SegmentTreeMain`.
- Tests under `src/test/java/advanced/data/structures/trees/segmenttree/`.
